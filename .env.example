# Django Settings
SECRET_KEY=django-insecure-dev-key-NOT-for-production-use
DEBUG=True

# Database - if working on this set it to your own local MariaDB credentials
DB_NAME=tossr_db
DB_USER=your_mariadb_username
DB_PASSWORD=your_mariadb_password
DB_HOST=localhost
DB_PORT=3306